h1
Цикли в JavaScript

p tag
Циклите в JavaScript се използват за намаляване на повтарящите се задачи чрез многократно изпълнение на блок от код, стига да е вярно определено условие. Това прави кода по-сбит и ефективен.
Да предположим, че искаме да отпечатаме „Hello World“ пет пъти. Вместо ръчно да пишем оператора print многократно, можем да използваме цикъл, за да автоматизираме задачата и да я изпълним въз основа на даденото условие.

code

for (let i = 0; i < 5; i++) {
    console.log("Hello World!");
}

h2 tag
For цикъл
Цикълът for повтаря блок от код определен брой пъти. Той съдържа инициализация, условие и инкремент/декремент в един ред.

Синтаксис
за (инициализация; условие; увеличение/намаляване) {
    // Код за изпълнение
}

image - https://media.geeksforgeeks.org/wp-content/uploads/loop2.png

code

for (le i = 0; i <= 3; i++){
	console.log("Count: ", i);
}

While цикъл
Цикълът while се изпълнява, докато условието е вярно. Може да се разглежда като повтарящ се if оператор.

Синтаксис
while (условие) { 
    // Код за изпълнение 
}

image - https://media.geeksforgeeks.org/wp-content/uploads/Loop1.png

code

let i = 0;
while (i < 3) {
    console.log("Number:", i);
    i++;
}


do-while цокъл
Цикълът do-while е подобен на цикъла while, с изключение на това, че изпълнява блока с код поне веднъж, преди да провери условието.

Синтаксис
do { 
    // Код за изпълнение 
} while (условие);

image - https://media.geeksforgeeks.org/wp-content/uploads/loop3.png

code

let i = 0;
do {
    console.log("Iteration:", i);
    i++;
} while (i < 3);

for in цокъл
Цикълът for...in се използва за итерация върху свойствата на обект. Той итерира само върху ключове на обект, чието свойство enumerable е зададено на „true“.

Синтаксис
for (let key in object) {
    // Code to execute
}

code

const obj = { name: "Ashish", age: 25 };
for (let key in obj) {
    console.log(key, ":", obj[key]);
}

for of цикъл
Цикълът for...of се използва за итериране върху итерируеми обекти като масиви, низове или множества. Той директно итерира стойността и има по-сбит синтаксис от цикъла for.

Синтаксис
for (let value of iterable) {
    // Code to execute
}

code

let a = [1, 2, 3, 4, 5];
for (let val of a) {
    console.log(val);
}


Кой цикъл да изберете?

    Използвайте цикъл for , когато броят на итерациите е известен.
    Използвайте цикъла while , когато условието зависи от динамични фактори.
    Използвайте цикъла do-while, за да гарантирате, че блокът ще се изпълни поне веднъж.
    Използвайте цикъла for...in за итерация през свойствата на обекта.
    Използвайте цикъла for...of за итериране през итерируеми обекти.


линк -https://www.geeksforgeeks.org/javascript/loops-in-javascript/